{/* _AUTOGENERATED   do not hand-edit. Run `python scripts/gen_dbt_reference.py` to regenerate. */}

# `dim_circuits`

One row per circuit × era. Physical properties used for fuel load estimation (weight_penalty_factor), track evolution (abrasiveness_index), and constructor pace decomposition (track_energy_index). Seeded from circuit_reference.csv   refresh when layouts change.

## Overview

| Property | Value |
|---|---|
| Layer | `reference` |
| Contract enforced | No |

## Columns

| Column | Type | Nullable | Tests | Description |
|---|---|---|---|---|
| `circuit_key` |   | Yes | `not_null`, `unique` | Stable identifier matching race_id in stg_laps |
| `fuel_consumption_rate_kg_per_lap` |   | Yes | `not_null` | Estimated kg/lap used for fuel_mass_kg calculation in Layer 03 |
| `weight_penalty_factor` |   | Yes | `not_null` | Seconds per kg of fuel mass carried. Formula: 0.02 + 0.0002 × corner_count × avg_lateral_g. Used in int_lap_fuel_state to compute weight_corrected_lap_time. |
| `lap_length_km` |   | Yes | `expect_column_values_to_be_between` | Circuit lap length in kilometers |
