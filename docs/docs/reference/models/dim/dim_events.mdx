{/* _AUTOGENERATED   do not hand-edit. Run `python scripts/gen_dbt_reference.py` to regenerate. */}

# `dim_events`

Race-level event dimension. One row per event (damage, retirement, penalty). Currently populated from the raw_dim_events seed (manual entries for 2021). Joined onto fct_driver_degradation (legacy) and fct_lap_residuals to flag performance-impact and reliability-impact events.

## Overview

| Property | Value |
|---|---|
| Layer | `marts` |
| Contract enforced | No |

## Columns

| Column | Type | Nullable | Tests | Description |
|---|---|---|---|---|
| `event_id` |   | Yes | `not_null`, `unique` |  |
| `race_id` |   | Yes | `not_null` |  |
