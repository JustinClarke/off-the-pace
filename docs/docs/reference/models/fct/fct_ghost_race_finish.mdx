{/* _AUTOGENERATED   do not hand-edit. Run `python scripts/gen_dbt_reference.py` to regenerate. */}

# `fct_ghost_race_finish`

Ghost car model (part 2). Race finish position simulator. Grain: (host_constructor_id, ego_driver_id, race_id). Ranks drivers by predicted cumulative race time within each (race, host_constructor) scenario. Filtered to recombination_confidence &gt;= 0.3.

## Overview

| Property | Value |
|---|---|
| Layer | `marts` |
| Contract enforced | No |
| Tags | `simulation`, `ghost_car` |
| Upstream | [`fct_ghost_car_pace`](./fct_ghost_car_pace), [`stg_laps`](../stg/stg_laps) |

## Columns

| Column | Type | Nullable | Tests | Description |
|---|---|---|---|---|
| `ghost_race_id` |   | Yes | `not_null`, `unique` | Surrogate PK = MD5(race_year \|\| race_id \|\| ego_driver_id \|\| host_constructor_id) |
| `predicted_finish_position` |   | Yes | `not_null`, `expect_column_values_to_be_between` | Projected finish position within this host-constructor scenario. |
| `actual_finish_position` |   | Yes |   | Actual finish position from stg_laps (last valid position). |
| `delta_vs_actual_position` |   | Yes | `not_null` | Projected minus actual finish position. Negative = would have finished higher. |
