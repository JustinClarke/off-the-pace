{/* _AUTOGENERATED   do not hand-edit. Run `python scripts/gen_dbt_reference.py` to regenerate. */}

# `int_constructor_structural_pace`

Constructor structural pace model. Causal constructor pace via within-constructor between-driver panel regression. One row per (race_year, race_id, constructor_id). Uses pyfixest high-dimensional fixed effects to estimate constructor coefficient α, controlling for driver, circuit, and lap-in-stint effects. Clustered standard errors by (race_id, constructor_id).

## Overview

| Property | Value |
|---|---|
| Layer | `intermediate` |
| Contract enforced | No |
| Tags | `causal_decomposition`, `constructor` |
| Upstream | [`int_lap_fuel_state`](./int_lap_fuel_state), [`int_field_pace_curve`](./int_field_pace_curve), [`int_stint_geometry`](./int_stint_geometry), [`stg_laps`](../stg/stg_laps), [`int_event_corrections`](./int_event_corrections), [`int_track_evolution`](./int_track_evolution) |
| Model-level tests | `expect_column_pair_values_A_to_be_greater_than_B` |

## Columns

| Column | Type | Nullable | Tests | Description |
|---|---|---|---|---|
| `constructor_race_id` |   | Yes | `not_null`, `unique` | Surrogate PK = hash(race_year, race_id, constructor_id) |
| `race_year` |   | Yes | `not_null` | Season year |
| `race_id` |   | Yes | `not_null` | Race identifier |
| `constructor_id` |   | Yes | `not_null` | Constructor identifier (FK to dim_constructors) |
| `constructor_structural_pace_s` |   | Yes | `not_null` | Fixed-effect coefficient α_constructor in seconds. Positive = slower than field. Identified via within-constructor, between-driver variation from teammate pairs. |
| `constructor_structural_pace_se_s` |   | Yes | `not_null`, `expect_column_values_to_be_between` | Clustered standard error (CRV1) by (race_id, constructor_id). Positive, ≥0. Captures uncertainty in α estimate. |
| `constructor_structural_pace_ci_low_s` |   | Yes | `not_null` | 95% confidence interval lower bound |
| `constructor_structural_pace_ci_high_s` |   | Yes | `not_null` | 95% confidence interval upper bound |
| `panel_observations_n` |   | Yes | `not_null` | Sample size of clean laps used in the regression |
| `clean_teammate_pair_laps_n` |   | Yes | `not_null` | Count of clean laps where both teammates in a constructor ran (used to assess pair quality). Flags constructor-seasons where this is &lt;100 laps. |
| `r_squared_within` |   | Yes | `expect_column_values_to_be_between` | Within fixed-effect R² (sanity check; low is expected, high is good). NULL until the pyfixest model is in place   a misleading 0.0 is worse than NULL. |
| `fit_timestamp` |   | Yes | `not_null` | UTC timestamp when this coefficient was fitted (ISO 8601) |
