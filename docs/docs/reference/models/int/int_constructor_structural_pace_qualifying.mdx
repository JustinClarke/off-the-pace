{/* _AUTOGENERATED   do not hand-edit. Run `python scripts/gen_dbt_reference.py` to regenerate. */}

# `int_constructor_structural_pace_qualifying`

Qualifying decomposition model (sub). Constructor pace coefficient for qualifying sessions. Mirrors int_constructor_structural_pace but fit on qualifying laps (is_personal_best = TRUE). The qualifying-trim coefficient is expected to differ from the race coefficient. One row per (race_year, race_id, constructor_id).

## Overview

| Property | Value |
|---|---|
| Layer | `intermediate` |
| Contract enforced | No |
| Tags | `simulation`, `qualifying` |
| Upstream | [`stg_laps_qualifying`](../stg/stg_laps_qualifying) |

## Columns

| Column | Type | Nullable | Tests | Description |
|---|---|---|---|---|
| `constructor_race_id` |   | Yes | `not_null`, `unique` | Surrogate PK = concat(race_year, race_id, constructor_id, '_Q') |
| `constructor_structural_pace_s` |   | Yes | `not_null` | Constructor qualifying-mode coefficient vs session median. Positive = slower. |
