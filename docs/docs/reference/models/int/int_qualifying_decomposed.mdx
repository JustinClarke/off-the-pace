{/* _AUTOGENERATED   do not hand-edit. Run `python scripts/gen_dbt_reference.py` to regenerate. */}

# `int_qualifying_decomposed`

Qualifying decomposition model. Public-facing qualifying residual decomposition. Adds quali_vs_race_skill_delta_s: differential between driver's qualifying and race skill. Positive = single-lap pace specialist (faster in quali relative to race management). Grain: lap_id   one row per valid qualifying lap.

## Overview

| Property | Value |
|---|---|
| Layer | `intermediate` |
| Contract enforced | No |
| Tags | `simulation`, `qualifying` |
| Upstream | [`int_lap_residual_decomposed_qualifying`](./int_lap_residual_decomposed_qualifying), [`int_lap_residual_decomposed`](./int_lap_residual_decomposed) |

## Columns

| Column | Type | Nullable | Tests | Description |
|---|---|---|---|---|
| `lap_id` |   | Yes | `not_null`, `unique` | PK, FK to stg_laps_qualifying |
| `quali_skill_residual_s` |   | Yes | `not_null` | Per-lap qualifying driver skill residual. |
| `quali_vs_race_skill_delta_s` |   | Yes |   | Per-driver per-event differential between qualifying and race skill. Positive = qualifying specialist. NULL when no race data available for that event. |
