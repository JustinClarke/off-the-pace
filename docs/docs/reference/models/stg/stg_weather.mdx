{/* _AUTOGENERATED   do not hand-edit. Run `python scripts/gen_dbt_reference.py` to regenerate. */}

# `stg_weather`

Weather snapshot per lap, joined from bronze weather samples (≈1Hz) using nearest-predecessor match on session_time_s. One row per driver × lap. Covers all 168 races 2018–2024; zero nulls in source data.

## Overview

| Property | Value |
|---|---|
| Layer | `staging` |
| Contract enforced | No |
| Upstream | [`stg_laps`](./stg_laps) |

## Columns

_Column definitions not yet documented in schema.yml._
