{/* _AUTOGENERATED   do not hand-edit. Run `python scripts/gen_schema_reference.py` to regenerate. */}

# Laps

Bronze-layer fact table. One row per driver per lap. ~20–30k rows per race (20 drivers × 50–70 laps). Hive-partitioned by season and race.

## Overview

| Property | Value |
|---|---|
| File pattern | `bronze/laps/season=YYYY/race=<slug>/YYYY_<slug>_laps.parquet` |
| Example row count | 2024 Bahrain: 23 drivers × 57 laps = ~1,311 rows |

:::note

LapTime is nanoseconds in Parquet; convert to seconds with / 1e9. Qualifying laps share this schema with session='Q'.

:::

## Columns

| Column | Type | Nullable | Source | Description |
|---|---|---|---|---|
| `DriverNumber` | `integer` | No | FastF1 native | FIA driver number (1–99). |
| `LapNumber` | `integer` | No | FastF1 native | 1-indexed lap number within the session. Includes outlap (lap 0) if present. |
| `LapTime` | `integer` | Yes | FastF1 native | Lap duration in nanoseconds. Null for in-progress or invalid laps. Convert to seconds in dbt: `LapTime / 1000000000.0`. |
| `Compound` | `string` | Yes | FastF1 native | Pirelli compound. Null if unknown (rare). |
| `TyreLife` | `integer` | Yes | FastF1 native | Consecutive laps on current tyre set. Resets to 1 on pit stop. |
| `FreshTyre` | `boolean` | Yes | FastF1 native | True if tyre age ≤ 1 lap. FastF1 infers from pit stop data. |
| `TrackStatus` | `string` | Yes | FastF1 native | Single-digit string. 1=green, 2=yellow, 3=virtual safety car, 4=safety car, 5=red, 6=VSC, 7=red+stopped. |
| `PitOutLap` | `boolean` | Yes | FastF1 native | True if driver exited the pit lane on this lap. |
| `PitInLap` | `boolean` | Yes | FastF1 native | True if driver pitted at the end of this lap. |
| `PitInTime` | `int64` | Yes | FastF1 native | Session elapsed time at pit lane entry in nanoseconds. Null if no pit-in this lap. |
| `PitOutTime` | `int64` | Yes | FastF1 native | Session elapsed time at pit lane exit in nanoseconds. Null if no pit-out this lap. |
| `Driver` | `string` | Yes | FastF1 native | Three-letter driver code (e.g. HAM, VER). |
| `Team` | `string` | Yes | FastF1 native | Constructor/team name as returned by FastF1. |
| `Stint` | `integer` | Yes | FastF1 native | Stint number within the race session, 1-indexed. Resets to 1 after a pit stop. |
| `Sector1Time` | `int64` | Yes | FastF1 native | Sector 1 duration in nanoseconds. Convert to seconds with / 1e9. |
| `Sector2Time` | `int64` | Yes | FastF1 native | Sector 2 duration in nanoseconds. Convert to seconds with / 1e9. |
| `Sector3Time` | `int64` | Yes | FastF1 native | Sector 3 duration in nanoseconds. Convert to seconds with / 1e9. |
| `Sector1SessionTime` | `int64` | Yes | FastF1 native | Session elapsed time at Sector 1 exit in nanoseconds. Used for air-gap joins. |
| `Sector2SessionTime` | `int64` | Yes | FastF1 native | Session elapsed time at Sector 2 exit in nanoseconds. |
| `Sector3SessionTime` | `int64` | Yes | FastF1 native | Session elapsed time at Sector 3 exit in nanoseconds. |
| `SpeedI1` | `float32` | Yes | FastF1 native | Speed at intermediate trap 1 in kph. |
| `SpeedI2` | `float32` | Yes | FastF1 native | Speed at intermediate trap 2 in kph. |
| `SpeedFL` | `float32` | Yes | FastF1 native | Speed at finish line trap in kph. |
| `SpeedST` | `float32` | Yes | FastF1 native | Speed at main straight speed trap in kph. |
| `IsPersonalBest` | `boolean` | Yes | FastF1 native | True if this lap is the driver's personal best for the session. |
| `Deleted` | `boolean` | Yes | FastF1 native | True if the lap time was deleted (e.g. track limits violation). |
| `Position` | `integer` | Yes | FastF1 native | Race position at end of lap. |
| `FastF1Generated` | `boolean` | Yes | FastF1 native | True if this lap row was synthesised by FastF1 rather than sourced from telemetry. |
| `LapStartTime` | `int64` | Yes | FastF1 native | Session elapsed time at lap start in nanoseconds. |
| `IsAccurate` | `boolean` | Yes | FastF1 native | True if telemetry coverage is complete for the lap. FastF1 internal flag. |
| `race_id` | `string` | No | Ingestion pipeline | Format: `YYYY_RoundNumber`. Links to telemetry and race_control. |
| `session` | `string` | No | Ingestion pipeline | 'R' for race, 'Q' for qualifying. |
| `season` | `integer` | No | Ingestion pipeline | Hive partition key. Prune all queries with this column. |

## Required Columns

These columns are always present (non-null, always ingested): `DriverNumber`, `LapNumber`, `race_id`, `season`, `session`.
