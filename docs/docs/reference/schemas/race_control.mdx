{/* _AUTOGENERATED   do not hand-edit. Run `python scripts/gen_schema_reference.py` to regenerate. */}

# Race Control

Bronze-layer event log. One row per race control message. ~50–200 rows per race (average ~100). Covers flags, incidents, safety car deployments, and procedure messages.

## Overview

| Property | Value |
|---|---|
| File pattern | `bronze/race_control/season=YYYY/race=<slug>/race_control.parquet` |
| Example row count | 2024 Bahrain: ~120 messages |

:::note

session_time_s is null for all 2024 races due to FastF1 v3.8.3 API change. Use `time` (HH:MM:SS) instead; join to laps via lap number for precise timing. `message` text is unstructured   use regex or fuzzy matching.

:::

## Columns

| Column | Type | Nullable | Source | Description |
|---|---|---|---|---|
| `lap` | `integer` | Yes | FastF1 native | Lap number when message triggered. Null for pre-race messages (parades, formation laps). |
| `time` | `string` | Yes | FastF1 native | Message timestamp in HH:MM:SS format (session time, not wall clock). Parse as needed. |
| `session_time_s` | `float64` | Yes | FastF1 native | Time in seconds since session start. **Null for all 2024 races** due to FastF1 v3.8.3 DatetimeProperties API change. Use `time` column instead. ⚠️ *Null for 2024 races (FastF1 v3.8.3 regression)* |
| `flag` | `string` | Yes | FastF1 native | Flag status: YELLOW, DOUBLE_YELLOW, RED, GREEN, CHECKERED. Null for non-flag messages. |
| `category` | `string` | Yes | FastF1 native | Message category: 'Weather', 'Incident', 'CarEvent', 'SafetyCar', 'Procedure', etc. |
| `message` | `string` | Yes | FastF1 native | Human-readable message text. Unstructured   values include 'Safety car deployed', 'Debris on track', 'Lapped car released'. Use regex or fuzzy matching. |
| `code` | `string` | Yes | FastF1 native | Internal race control code. Format varies. |
| `race_id` | `string` | No | Ingestion pipeline | Links to `laps.race_id`. Format: `YYYY_RoundNumber`. |
| `season` | `integer` | No | Ingestion pipeline | Hive partition key. |

## Required Columns

These columns are always present (non-null, always ingested): `race_id`, `season`.
