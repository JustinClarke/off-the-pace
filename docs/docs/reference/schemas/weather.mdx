{/* _AUTOGENERATED   do not hand-edit. Run `python scripts/gen_schema_reference.py` to regenerate. */}

# Weather

Bronze-layer dimension. One row per sample   typically one per minute during the session. ~150–300 rows per race day.

## Overview

| Property | Value |
|---|---|
| File pattern | `bronze/weather/season=YYYY/race=<slug>/weather.parquet` |
| Example row count | 2024 Bahrain race day: ~300 samples |

:::note

rainfall_flag is boolean (true = precipitation detected). wind_direction is int16 (0–360). wind_speed_ms is in metres per second.

:::

## Columns

| Column | Type | Nullable | Source | Description |
|---|---|---|---|---|
| `ambient_temp_c` | `float32` | Yes | FastF1 native | Ambient (air) temperature in Celsius. |
| `track_temp_c` | `float32` | Yes | FastF1 native | Track surface temperature in Celsius. Typically 10–20°C higher than ambient on sunny days. |
| `humidity_pct` | `float32` | Yes | FastF1 native | Relative humidity, 0–100%. |
| `rainfall_flag` | `bool` | Yes | FastF1 native | True if precipitation is detected during this sample period. |
| `wind_direction` | `int16` | Yes | FastF1 native | Wind direction in degrees (0–360°). Null if calm. |
| `wind_speed_ms` | `float32` | Yes | FastF1 native | Wind speed in metres per second. |
| `pressure_hpa` | `float32` | Yes | FastF1 native | Atmospheric pressure in hectopascals. |
| `session_time_s` | `float64` | Yes | Ingestion pipeline | Session elapsed time in seconds when this sample was recorded. |
| `session` | `string` | No | Ingestion pipeline | 'R' for race, 'Q', 'FP1', 'FP2', 'FP3'. |
| `race_id` | `string` | No | Ingestion pipeline | Links to `laps.race_id`. Format: `YYYY_RoundNumber`. |
| `season` | `integer` | No | Ingestion pipeline | Hive partition key. |

## Required Columns

These columns are always present (non-null, always ingested): `race_id`, `season`, `session`.
