---
title: "trailing_observation_count"
sidebarTitle: "trailing_observation_count"
description: "How many valid observations the matching trailing_median() actually saw."
---

{/* _AUTOGENERATED   do not hand-edit. Run `python scripts/gen_macro_reference.py` to regenerate. */}

How many valid observations the matching trailing_median() actually saw. Ship it beside every trailing_median in a model that feeds the ML contract: it is what makes that median's NULLs and its early-window instability visible to a consumer instead of silent.

## Signature

```sql
{{ trailing_observation_count(value_col, partition_by, order_by, lookback=none, frame='rows', valid_condition=none) }}
```

## Returns

scalar expression composable in a SELECT list.
