---
title: "trailing_value_expr"
sidebarTitle: "trailing_value_expr"
description: "The value a trailing window aggregates: `value_col`, masked to NULL where the row is not a valid observation."
---

{/* _AUTOGENERATED   do not hand-edit. Run `python scripts/gen_macro_reference.py` to regenerate. */}

The value a trailing window aggregates: `value_col`, masked to NULL where the row is not a valid observation. CASE, not FILTER -- DuckDB does not support FILTER in ordered-set aggregates inside window functions, which is why int_lap_thermal_proxy used to pre-aggregate with a GROUP BY and reach forward as a result.

## Signature

```sql
{{ trailing_value_expr(value_col, valid_condition=none) }}
```
