---
title: "int_sc_hazard_history"
sidebarTitle: "int_sc_hazard_history"
description: "Safety-Car / Virtual-Safety-Car base rate per circuit (venue slug), as a hazard per racing lap."
---

{/* _AUTOGENERATED   do not hand-edit. Run `python scripts/gen_dbt_reference.py` to regenerate. */}

Safety-Car / Virtual-Safety-Car base rate per circuit (venue slug), as a hazard per racing lap. Estimated from SC/VSC deployment onsets in stg_track_status with racing-lap exposure from stg_laps, over the set of races that have an ingested track_status timeline (partial during the v0.2 backfill). Raw and empirical-Bayes-shrunk rates are provided. Feeds the MC finish-order core (transform-v0.2 §4.5).

## Overview

| Property | Value |
|---|---|
| Layer | `intermediate` |
| Contract enforced | No |
| Upstream | [`stg_track_status`](../stg/stg_track_status), [`stg_laps`](../stg/stg_laps) |

## Columns

| Column | Type | Nullable | Tests | Description |
|---|---|---|---|---|
| `circuit_slug` |   | Yes | `not_null`, `unique` | Venue identifier (race name slug, e.g. monaco_grand_prix); recurs across seasons. |
| `n_races` |   | Yes | `not_null` | Number of races with a track_status timeline observed at this circuit. |
| `racing_laps` |   | Yes | `not_null`, `expect_column_values_to_be_between` | Total racing-lap exposure (sum of per-race leader lap counts) the hazard denominator. |
| `sc_hazard_per_lap` |   | Yes | `not_null`, `expect_column_values_to_be_between` | Raw Safety-Car onsets per racing lap. |
| `vsc_hazard_per_lap` |   | Yes | `not_null`, `expect_column_values_to_be_between` | Raw Virtual-Safety-Car onsets per racing lap. |
| `any_hazard_per_lap` |   | Yes | `not_null`, `expect_column_values_to_be_between` | Raw combined SC+VSC onsets per racing lap. |
| `sc_hazard_per_lap_shrunk` |   | Yes | `not_null`, `expect_column_values_to_be_between` | Empirical-Bayes SC hazard shrunk toward the global pooled rate (pseudo-count sc_hazard_prior_laps). |
| `vsc_hazard_per_lap_shrunk` |   | Yes | `not_null`, `expect_column_values_to_be_between` | Empirical-Bayes VSC hazard shrunk toward the global pooled rate. |
| `any_hazard_per_lap_shrunk` |   | Yes | `not_null`, `expect_column_values_to_be_between` | Empirical-Bayes combined SC+VSC hazard shrunk toward the global pooled rate. |
