---
title: "stg_tyre_allocations"
sidebarTitle: "stg_tyre_allocations"
description: "STUB source not yet ingested."
---

{/* _AUTOGENERATED   do not hand-edit. Run `python scripts/gen_dbt_reference.py` to regenerate. */}

<Note>Part of the [Staging](/transform/families/staging) family.</Note>

STUB   source not yet ingested. Returns an empty result set with the correct schema so downstream models (dim_compounds_season) can reference it without build errors. FastF1 already exposes HARD/MEDIUM/SOFT labels directly in the Compound column, so this model is low priority for 2018+ seasons. Activate when Pirelli allocation sheets are scraped to map compound codes (C1–C5) to hard/medium/soft per circuit.

## Lineage

```mermaid
flowchart LR
  stg_tyre_allocations
  style stg_tyre_allocations fill:#e40404,stroke:#e40404,color:#fff
```

## Overview

| Property | Value |
|---|---|
| Layer | `staging` |
| Family | [Staging](/transform/families/staging) |
| Contract enforced | No |

**0 tests** [guard](/transform/ci/overview) this model  -  0 generic, 0 singular.

## Columns

| Column | Type | Nullable | Tests | Description |
|---|---|---|---|---|
| `race_year` | `integer` | Yes |   | Season year (always NULL in the stub). |
| `circuit_key` | `varchar` | Yes |   | Event/grand-prix key (always NULL in the stub). |
| `compound_code` | `varchar` | Yes |   | Pirelli compound code C1–C5 (always NULL in the stub). |
| `compound_label` | `varchar` | Yes |   | hard/medium/soft label the code maps to (always NULL in the stub). |
| `allocated_sets_per_driver` | `integer` | Yes |   | Number of sets of this compound each driver is allocated (always NULL in the stub). |
