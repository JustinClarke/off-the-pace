# Terraform: never commit state (may contain resource metadata) or local secrets.
.terraform/
.terraform.lock.hcl
*.tfstate
*.tfstate.*
terraform.tfvars
*.auto.tfvars
crash.log
override.tf
override.tf.json
